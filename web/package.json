{
  "name": "influence-web",
  "version": "1.0.0",
  "private": true,
  "description": "Static frontend + minimal serverless API layer for Influence (creator email notifications, contact opt-in).",
  "scripts": {
    "postinstall": "prisma generate",
    "db:push": "prisma db push"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "ethers": "^6.17.0",
    "resend": "^4.0.1"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
